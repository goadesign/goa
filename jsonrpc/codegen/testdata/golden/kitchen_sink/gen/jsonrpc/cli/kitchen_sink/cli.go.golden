// Code generated by goa, DO NOT EDIT.
//
// kitchen-sink JSON-RPC client CLI support package
//
// Command:
// goa

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	calcc "generated.local/gen/jsonrpc/calc/client"
	feedc "generated.local/gen/jsonrpc/feed/client"
	mixedc "generated.local/gen/jsonrpc/mixed/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"calc (add|ping|log)",
		"feed (watch|snapshot)",
		"mixed lookup",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "calc add --body '{\n      \"a\": 5718665059814127631,\n      \"b\": 5455021967244844938\n   }' --request-id \"Necessitatibus perferendis autem dolor.\"" + "\n" +
		os.Args[0] + " " + "feed watch --last-event-id \"Eum in.\" --request-id \"Dolor consequatur dolor cum commodi saepe.\"" + "\n" +
		os.Args[0] + " " + "mixed lookup --body '{\n      \"key\": \"Earum amet voluptatum ad soluta.\"\n   }' --request-id \"Deserunt non maxime tempora alias qui.\"" + "\n" +
		""
}

// cliStringFlag keeps an omitted command-line flag distinct from an explicitly empty flag.
type cliStringFlag struct {
	value *string
}

// String returns the flag text shown by the standard flag package.
func (f *cliStringFlag) String() string {
	if f.value == nil {
		return ""
	}
	return *f.value
}

// Set records that the user supplied the flag, even when value is empty.
func (f *cliStringFlag) Set(value string) error {
	f.value = &value
	return nil
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		calcFlags = flag.NewFlagSet("calc", flag.ContinueOnError)

		calcAddFlags         = flag.NewFlagSet("add", flag.ExitOnError)
		calcAddBodyFlag      = new(cliStringFlag)
		calcAddRequestIDFlag = new(cliStringFlag)

		calcPingFlags = flag.NewFlagSet("ping", flag.ExitOnError)

		calcLogFlags         = flag.NewFlagSet("log", flag.ExitOnError)
		calcLogBodyFlag      = new(cliStringFlag)
		calcLogRequestIDFlag = new(cliStringFlag)

		feedFlags = flag.NewFlagSet("feed", flag.ContinueOnError)

		feedWatchFlags           = flag.NewFlagSet("watch", flag.ExitOnError)
		feedWatchLastEventIDFlag = new(cliStringFlag)
		feedWatchRequestIDFlag   = new(cliStringFlag)

		feedSnapshotFlags         = flag.NewFlagSet("snapshot", flag.ExitOnError)
		feedSnapshotRequestIDFlag = new(cliStringFlag)

		mixedFlags = flag.NewFlagSet("mixed", flag.ContinueOnError)

		mixedLookupFlags         = flag.NewFlagSet("lookup", flag.ExitOnError)
		mixedLookupBodyFlag      = new(cliStringFlag)
		mixedLookupRequestIDFlag = new(cliStringFlag)
	)
	calcAddFlags.Var(calcAddBodyFlag, "body", "")
	calcAddFlags.Var(calcAddRequestIDFlag, "request-id", "Request ID")
	calcLogFlags.Var(calcLogBodyFlag, "body", "")
	calcLogFlags.Var(calcLogRequestIDFlag, "request-id", "Optional caller-supplied request ID")
	feedWatchFlags.Var(feedWatchLastEventIDFlag, "last-event-id", "")
	feedWatchFlags.Var(feedWatchRequestIDFlag, "request-id", "Request ID")
	feedSnapshotFlags.Var(feedSnapshotRequestIDFlag, "request-id", "Request ID")
	mixedLookupFlags.Var(mixedLookupBodyFlag, "body", "")
	mixedLookupFlags.Var(mixedLookupRequestIDFlag, "request-id", "Request ID")

	calcFlags.Usage = calcUsage
	calcAddFlags.Usage = calcAddUsage
	calcPingFlags.Usage = calcPingUsage
	calcLogFlags.Usage = calcLogUsage

	feedFlags.Usage = feedUsage
	feedWatchFlags.Usage = feedWatchUsage
	feedSnapshotFlags.Usage = feedSnapshotUsage

	mixedFlags.Usage = mixedUsage
	mixedLookupFlags.Usage = mixedLookupUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "calc":
			svcf = calcFlags
		case "feed":
			svcf = feedFlags
		case "mixed":
			svcf = mixedFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "calc":
			switch epn {
			case "add":
				epf = calcAddFlags

			case "ping":
				epf = calcPingFlags

			case "log":
				epf = calcLogFlags

			}

		case "feed":
			switch epn {
			case "watch":
				epf = feedWatchFlags

			case "snapshot":
				epf = feedSnapshotFlags

			}

		case "mixed":
			switch epn {
			case "lookup":
				epf = mixedLookupFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "calc":
			c := calcc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "add":
				endpoint = c.Add()
				data, err = calcc.BuildAddPayload(calcAddBodyFlag.value, calcAddRequestIDFlag.value)
			case "ping":
				endpoint = c.Ping()
			case "log":
				endpoint = c.Log()
				data, err = calcc.BuildLogPayload(calcLogBodyFlag.value, calcLogRequestIDFlag.value)
			}
		case "feed":
			c := feedc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "watch":
				endpoint = c.Watch()
				data, err = feedc.BuildWatchPayload(feedWatchLastEventIDFlag.value, feedWatchRequestIDFlag.value)
			case "snapshot":
				endpoint = c.Snapshot()
				data, err = feedc.BuildSnapshotPayload(feedSnapshotRequestIDFlag.value)
			}
		case "mixed":
			c := mixedc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "lookup":
				endpoint = c.Lookup()
				data, err = mixedc.BuildLookupPayload(mixedLookupBodyFlag.value, mixedLookupRequestIDFlag.value)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// calcUsage displays the usage of the calc command and its subcommands.
func calcUsage() {
	fmt.Fprintln(os.Stderr, `Service is the Calc service interface.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] calc COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    add: Add implements add.`)
	fmt.Fprintln(os.Stderr, `    ping: Ping implements ping.`)
	fmt.Fprintln(os.Stderr, `    log: Log implements log.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s calc COMMAND --help\n", os.Args[0])
}
func calcAddUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] calc add", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -request-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Add implements add.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -request-id STRING: Request ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "calc add --body '{\n      \"a\": 5718665059814127631,\n      \"b\": 5455021967244844938\n   }' --request-id \"Necessitatibus perferendis autem dolor.\"")
}

func calcPingUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] calc ping", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Ping implements ping.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "calc ping")
}

func calcLogUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] calc log", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -request-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Log implements log.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -request-id STRING: Optional caller-supplied request ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "calc log --body '{\n      \"message\": \"Quo excepturi.\"\n   }' --request-id \"Rerum aut.\"")
}

// feedUsage displays the usage of the feed command and its subcommands.
func feedUsage() {
	fmt.Fprintln(os.Stderr, `Service is the Feed service interface.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] feed COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    watch: Watch implements watch.`)
	fmt.Fprintln(os.Stderr, `    snapshot: Snapshot implements snapshot.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s feed COMMAND --help\n", os.Args[0])
}
func feedWatchUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] feed watch", os.Args[0])
	fmt.Fprint(os.Stderr, " -last-event-id STRING")
	fmt.Fprint(os.Stderr, " -request-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Watch implements watch.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -last-event-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -request-id STRING: Request ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "feed watch --last-event-id \"Eum in.\" --request-id \"Dolor consequatur dolor cum commodi saepe.\"")
}

func feedSnapshotUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] feed snapshot", os.Args[0])
	fmt.Fprint(os.Stderr, " -request-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Snapshot implements snapshot.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -request-id STRING: Request ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "feed snapshot --request-id \"Et nulla modi necessitatibus ipsam quia.\"")
}

// mixedUsage displays the usage of the mixed command and its subcommands.
func mixedUsage() {
	fmt.Fprintln(os.Stderr, `Service is the Mixed service interface.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] mixed COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    lookup: Lookup implements lookup.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s mixed COMMAND --help\n", os.Args[0])
}
func mixedLookupUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] mixed lookup", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -request-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Lookup implements lookup.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -request-id STRING: Request ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "mixed lookup --body '{\n      \"key\": \"Earum amet voluptatum ad soluta.\"\n   }' --request-id \"Deserunt non maxime tempora alias qui.\"")
}
