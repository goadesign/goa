// Code generated by goa, DO NOT EDIT.
//
// Mixed client HTTP transport
//
// Command:
// goa

package client

import (
	"context"
	"net/http"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// Client lists the Mixed service endpoint HTTP clients.
type Client struct {
	// Lookup Doer is the HTTP client used to make requests to the lookup endpoint.
	LookupDoer goahttp.Doer

	// RestoreResponseBody controls whether the response bodies are reset after
	// decoding so they can be read again.
	RestoreResponseBody bool

	scheme  string
	host    string
	encoder func(*http.Request) goahttp.Encoder
	decoder func(*http.Response) goahttp.Decoder
}

// NewClient instantiates HTTP clients for all the Mixed service servers.
func NewClient(
	scheme string,
	host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restoreBody bool,
) *Client {
	return &Client{
		LookupDoer:          doer,
		RestoreResponseBody: restoreBody,
		scheme:              scheme,
		host:                host,
		decoder:             dec,
		encoder:             enc,
	}
}

// Lookup returns an endpoint that makes HTTP requests to the Mixed service
// lookup server.
func (c *Client) Lookup() goa.Endpoint {
	var (
		encodeRequest  = EncodeLookupRequest(c.encoder)
		decodeResponse = DecodeLookupResponse(c.decoder, c.RestoreResponseBody)
	)
	return func(ctx context.Context, v any) (any, error) {
		req, err := c.BuildLookupRequest(ctx, v)
		if err != nil {
			return nil, err
		}
		err = encodeRequest(req, v)
		if err != nil {
			return nil, err
		}
		resp, err := c.LookupDoer.Do(req)
		if err != nil {
			return nil, goahttp.ErrRequestError("Mixed", "lookup", err)
		}
		return decodeResponse(resp)
	}
}
