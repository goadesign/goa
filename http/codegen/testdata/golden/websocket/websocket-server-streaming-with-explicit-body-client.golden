// Code generated by goa, DO NOT EDIT.
//
// TestService WebSocket client streaming
//
// Command:
// goa

package client

import (
	"context"
	testservice "generated.local/gen/test_service"
	testserviceviews "generated.local/gen/test_service/views"
	"github.com/gorilla/websocket"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
	"io"
	"net/http"
	"sync"
	"time"
)

// ConnConfigurer holds the websocket connection configurer functions for the
// streaming endpoints in "TestService" service.
type ConnConfigurer struct {
	StreamUserIDFn goahttp.ConnConfigureFunc
}

// StreamUserIDClientStream implements the testservice.StreamUserIDClientStream
// interface.
type StreamUserIDClientStream struct {
	// conn is the underlying websocket connection.
	conn *websocket.Conn
	// view is the result view used to decode values received from the websocket
	// connection.
	view string
}

// NewConnConfigurer initializes the websocket connection configurer function
// with fn for all the streaming endpoints in "TestService" service.
func NewConnConfigurer(fn goahttp.ConnConfigureFunc) *ConnConfigurer {
	return &ConnConfigurer{
		StreamUserIDFn: fn,
	}
}

// Recv reads instances of "testservice.User" from the "StreamUserID" endpoint
// websocket connection.
func (s *StreamUserIDClientStream) Recv() (*testservice.User, error) {
	var (
		rv   *testservice.User
		body StreamUserIDResponseBody
		err  error
	)
	view := s.view
	if view == "" {
		view = "default"
	}
	err = s.conn.ReadJSON(&body)
	if websocket.IsCloseError(err, websocket.CloseNormalClosure) {
		s.conn.Close()
		return rv, io.EOF
	}
	if err != nil {
		return rv, err
	}
	err = ValidateStreamUserIDResponseBody(&body)
	if err != nil {
		return rv, err
	}
	res := NewStreamUserIDUserOK(&body)
	vres := &testserviceviews.User{Projected: res, View: view}
	if err := testserviceviews.ValidateUser(vres); err != nil {
		return rv, goahttp.ErrValidationError("TestService", "StreamUserID", err)
	}
	return testservice.NewUser(vres), nil
}

// RecvWithContext reads instances of "testservice.User" from the
// "StreamUserID" endpoint websocket connection with context.
func (s *StreamUserIDClientStream) RecvWithContext(ctx context.Context) (*testservice.User, error) {
	return s.Recv()
}

// SetView sets the result view used to decode values received from the
// "StreamUserID" endpoint websocket connection.
func (s *StreamUserIDClientStream) SetView(view string) {
	s.view = view
}
